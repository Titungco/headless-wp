[global]
daemonize = no
error_log = /dev/stderr
; No `pid` directive at all — an empty value doesn't disable the pid file
; (it still resolves to the compiled-in default, a directory that doesn't
; exist on this read-only filesystem); omitting the directive does.

include=/usr/local/etc/php-fpm.d/*.conf
